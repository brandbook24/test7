const TIMEOUT = 900;




const AppRoute = {
  Initial: 'initial',
  Game: 'game',
  Results: 'results',
};

// создадим константу для списка типов игры
// каждый элемент содержит название набора данных и подпись для кнопки


const GAME_TYPES = [

{ type: 'images', icon: 'images', label:'Начать игру' },

];



// перемешиваем набор данных для усложнения игры

const RANDOMIZED = false;
window.TIMEOUT = TIMEOUT;

window.AppRoute = AppRoute;
window.GAME_TYPES = GAME_TYPES;
window.RANDOMIZED = RANDOMIZED;

